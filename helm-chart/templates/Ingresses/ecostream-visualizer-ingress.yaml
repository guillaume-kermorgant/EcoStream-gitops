apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ecostream-visualizer-ingress
  namespace: "{{ .Values.namespace }}"
  annotations:
    kubernetes.io/ingress.class: alb
    # alb.ingress.kubernetes.io/target-type: ip
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTPS":443}]'
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/ssl-redirect: '443'
{{- if .Values.settings.ecostreamHostname }}
    external-dns.alpha.kubernetes.io/hostname: "{{ .Values.settings.ecostreamHostname }}"
{{- end  }}
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: "{{ .Values.components.ecostreamVisualizer.serviceName }}"
                port:
                  number: {{ .Values.components.ecostreamVisualizer.port }}
{{- if .Values.settings.ecostreamHostname }}
      host: "{{ .Values.settings.ecostreamHostname }}" # TODO: 
{{- end  }}
